version: 2.1
jobs:
  build:
    docker:
      - image: moneymanagerex/mmex_build_env:ubuntu.bionic
    steps: 
      - run:
          command: |
            cd $HOME;
            git config --global http.sslVerify false
            git clone --recurse https://github.com/wxWidgets/wxWidgets.git --branch v3.2.0 --single-branch
            cd wxWidgets;
            mkdir build-linux;
            cd build-linux;
            ../configure --disable-shared --enable-cxx11 --with-cxx=11 --enable-webview --with-gtk=3 --enable-unicode --enable-intl;
            make;